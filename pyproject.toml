[project]
name = "dotnix"
version = "0.1.0"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["scripts"]

[tool.pyright]
venvPath = "."
venv = ".venv"
pythonVersion = "3.15"
typeCheckingMode = "strict"

[tool.ruff]
target-version = "py315"
